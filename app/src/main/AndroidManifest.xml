<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/menuready"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/menuready"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kursach"
        tools:targetApi="31">
        <activity
            android:name=".DrinksListActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".DishSelectionActivity"/>
        <activity
            android:name=".OrderActivity"
            android:theme="@style/Theme.AppCompat"
            android:exported="false"/>
        <activity
        android:name=".LoginActivity"
        android:theme="@style/Theme.AppCompat"
        android:exported="true"/>
        <activity
            android:name=".BarActivity"
            android:theme="@style/Theme.AppCompat"
            android:exported="true"/>

        <activity
            android:name=".ChooseTableActivity"
            android:theme="@style/Theme.AppCompat"
            android:exported="true" >


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>